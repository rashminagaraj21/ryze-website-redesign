# Get-Ryze.ai Website Redesign

A modern, responsive frontend-only website built with HTML, CSS, and JavaScript.

## 📁 Project Structure

```
ryze-website-redesign/
│
├── index.html          # Main HTML file (structure/content)
├── css/
│   └── style.css      # All styling and design
├── js/
│   └── main.js        # Interactive features
└── README.md          # This file
```

## 🚀 Getting Started

1. **Open the website:**
   - Simply open `index.html` in your web browser
   - Or use a local server (recommended):
     - If you have Python: `python -m http.server 8000`
     - Then visit: `http://localhost:8000`

2. **No installation needed!**
   - This is a pure frontend project
   - No frameworks, no build tools, no dependencies
   - Just HTML, CSS, and JavaScript

## 📖 What Each File Does

### `index.html`
- Contains the structure and content of the website
- Defines all sections: header, hero, features, testimonials, pricing, footer
- Links to CSS and JavaScript files

### `css/style.css`
- All the visual styling (colors, fonts, layout)
- Makes the site responsive (works on mobile, tablet, desktop)
- Uses modern CSS features like Flexbox and Grid

### `js/main.js`
- Adds interactivity to the website
- Handles smooth scrolling navigation
- Controls mobile menu toggle
- Highlights active section in navigation

## 🎨 Features

- ✅ Fully responsive (mobile, tablet, desktop)
- ✅ Modern, clean design
- ✅ Smooth scrolling navigation
- ✅ Mobile-friendly menu
- ✅ Professional SaaS layout
- ✅ Beginner-friendly code with comments

## 📝 Learning Resources

### HTML Basics
- `<header>`, `<section>`, `<footer>` - Semantic HTML elements
- `id` and `class` attributes - Used to identify and style elements
- Links (`<a>`) - Navigation between sections

### CSS Basics
- Selectors (`.class`, `#id`) - Target specific elements
- Flexbox - For horizontal/vertical layouts
- Grid - For complex multi-column layouts
- Media queries (`@media`) - Make design responsive

### JavaScript Basics
- `querySelector` - Find elements in HTML
- `addEventListener` - React to user actions (clicks, scrolls)
- `classList` - Add/remove CSS classes dynamically

## 🔧 Customization

### Change Colors
Edit the CSS variables at the top of `css/style.css`:
```css
:root {
    --primary-color: #6366f1;  /* Change this to your brand color */
    --secondary-color: #8b5cf6;
}
```

### Add New Sections
1. Add HTML in `index.html`
2. Add corresponding styles in `css/style.css`
3. Update navigation links if needed

### Modify Content
- Edit text directly in `index.html`
- All content is clearly marked with comments

## 📱 Testing

Test your website on:
- Desktop browsers (Chrome, Firefox, Safari, Edge)
- Mobile devices (or use browser's device emulator)
- Different screen sizes

## 💡 Tips for Beginners

1. **Start small:** Make one change at a time
2. **Use browser DevTools:** Right-click → Inspect to see how elements work
3. **Read the comments:** Every section has explanations
4. **Experiment:** Change values and see what happens!

## 🎯 Next Steps

- Add more sections
- Customize colors and fonts
- Add animations
- Integrate with a backend (when ready)

---

**Happy coding! 🚀**





